package com.example.algamoney.api.evento.listener;

import javax.servlet.http.HttpServletResponse;

import org.springframework.context.ApplicationListener;
import org.springframework.stereotype.Component;

import com.example.algamoney.api.evento.RecursoCriadoEvent;

@Component
public class RecursoCriadoListener implements ApplicationListener<RecursoCriadoEvent>{

	@Override
	public void onApplicationEvent(RecursoCriadoEvent recursoCriadoListener) {
		HttpServletResponse response = recursoCriadoListener.getResponse();
		Long codigo = recursoCriadoListener.getCodigo();
		
		
		
	}

}
